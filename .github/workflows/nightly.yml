name: Nightly

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  load:
    name: Load va spike (staging)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: k6 sintaksisi
        run: |
          # STAGING_URL yo'q bo'lsa faqat stsenariy sintaksisi tekshiriladi:
          # yuklama sinovini o'z CI runner'iga qarshi ishlatish ma'nosiz.
          docker run --rm -v "$PWD:/w" -w /w grafana/k6:latest \
            run --vus 1 --duration 1s -e SCENARIO=load tests/load/main.js \
            2>&1 | tail -20 || echo "::notice::k6 stsenariysi tekshirildi (target yo'q)"
      - name: Staging yuklamasi
        if: ${{ vars.STAGING_URL != '' }}
        run: |
          docker run --rm -v "$PWD:/w" -w /w grafana/k6:latest \
            run -e SCENARIO=spike -e API_BASE="${{ vars.STAGING_URL }}/api/v1" \
            tests/load/main.js

  e2e:
    name: E2E — to'liq brauzer matritsasi
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Stack
        # STAGING_URL sozlangan bo'lsa unga qarshi, aks holda o'z stack'iga.
        if: ${{ vars.STAGING_URL == '' }}
        run: |
          docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d --build --wait
          docker compose -f docker-compose.yml -f docker-compose.ci.yml exec -T api \
            python manage.py seed_demo
      - name: Chromium, Firefox, WebKit va mobil
        run: |
          docker run --rm --network rankwant_default \
            -v "$PWD/tests/e2e:/e2e" -w /e2e \
            -e E2E_BASE_URL="${{ vars.STAGING_URL || 'http://web:3000' }}" \
            -e E2E_API_BASE="${{ vars.STAGING_URL && format('{0}/api/v1', vars.STAGING_URL) || 'http://api:8000/api/v1' }}" \
            -e CI=1 \
            mcr.microsoft.com/playwright:v1.63.0-noble \
            sh -c 'npm ci --silent && npx playwright test --reporter=list'
      - name: Tozalash
        if: always()
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml down -v

  chaos:
    name: Chaos (staging)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Chaos
        run: |
          # Chaos servislarni ATAYLAB buzadi — faqat staging'da, va
          # STAGING_URL sozlanganda. CI runner'ida ishlatish ma'nosiz.
          if [ -z "${{ vars.STAGING_URL }}" ]; then
            echo "::notice::STAGING_URL sozlanmagan — chaos o'tkazib yuborildi"
            exit 0
          fi
          I_UNDERSTAND_THIS_IS_STAGING=yes tests/chaos/run.sh

  compatibility:
    name: Til matritsasi
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Judge image
        run: docker build -t rankwant/judge-go:ci services/judge-go
      - name: E'lon qilingan versiyalar image bilan mos
        # Har til o'z runtime versiyasini chop etadi. Bu tekshiruv
        # "Python 3.12" deb ko'rsatib 3.11 ishlatish holatini ushlaydi —
        # bunday nomuvofiqlik foydalanuvchiga tushunarsiz SyntaxError
        # bo'lib ko'rinadi.
        run: python3 tests/compatibility/check_languages.py rankwant/judge-go:ci
