/** `duels` — feature'ning ommaviy yuzasi.
 *
 * Tashqaridan faqat shu fayl orqali import qilinadi:
 * `import { X } from "@/features/duels"`.
 * Ichki tuzilma (`components/`, `api/`) — xususiy.
 */

export { DuelActions } from "./components/DuelActions";

export { DuelDetail } from "./components/DuelDetail";
