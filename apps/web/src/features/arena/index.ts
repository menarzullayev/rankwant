/** `arena` — feature'ning ommaviy yuzasi.
 *
 * Tashqaridan faqat shu fayl orqali import qilinadi:
 * `import { X } from "@/features/arena"`.
 * Ichki tuzilma (`components/`, `api/`) — xususiy.
 */

export { ArenaPlayer } from "./components/ArenaPlayer";
