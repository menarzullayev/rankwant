/** `quizzes` — feature'ning ommaviy yuzasi.
 *
 * Tashqaridan faqat shu fayl orqali import qilinadi:
 * `import { X } from "@/features/quizzes"`.
 * Ichki tuzilma (`components/`, `api/`) — xususiy.
 */

export { QuizPlayer } from "./components/QuizPlayer";
