# Django 5.2 — LTS, ADR-0003 da ataylab tanlangan (6.x emas).
# Qolgan paketlarda pastki chegara qo'yiladi: xotiradan yozilgan aniq
# versiya eskirgan va CVE'li bo'lib chiqishi mumkin (2026-09-06 da
# djangorestframework 3.15.2 va next 15.1.3 aynan shunday bo'ldi).
#
# Bu fayl — NIYAT. Docker va CI `requirements.lock` dan o'rnatadi,
# ya'ni hamma bir xil versiyani oladi. Yangilash:
#   uv pip compile requirements.txt -o requirements.lock
#   uv pip compile requirements-dev.txt -o requirements-dev.lock
Django>=5.2.17,<6.0
djangorestframework>=3.17.2
drf-spectacular>=0.27
django-filter>=24
psycopg[binary]>=3.2
redis>=5.2
celery>=5.4
django-cors-headers>=4.6
gunicorn>=23
boto3>=1.35
